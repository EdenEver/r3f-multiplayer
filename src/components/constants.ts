import { Vector3 } from "three"

export const CAMERA_DISTANCE = 100
export const CAMERA_OFFSET = new Vector3(CAMERA_DISTANCE, CAMERA_DISTANCE, CAMERA_DISTANCE)
export const CAMERA_ZOOM = 50
export const CAMERA_NEAR = 0
